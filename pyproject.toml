[project]
name = "rezn1r-blog-builder"
version = "0.1.0"
description = "Pelican build setup for Markdown blog posts"
requires-python = ">=3.10"
dependencies = [
    "pelican[markdown]>=4.11.0",
]

[tool.uv]
package = false
